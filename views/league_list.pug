extends layout

block content
  h1= title

  ul
    each league in league_list
      li
        a(href=league.url) #{league.name}
        |   (Football league of: #{league.country})

    else
      li There are no leagues.
