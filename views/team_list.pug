extends layout

block content
  h1= title

  ul
    each team in team_list
      li
        a(href=team.url) #{team.name}
        |  (#{team.league.name}) From: #{team.city}

    else
      li There are no authors.
